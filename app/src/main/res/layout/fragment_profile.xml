<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    tools:context="com.prodevsmx.rider.fragments.FragmentProfile">

    <com.facebook.login.widget.ProfilePictureView
        android:id="@+id/ppvProfile"
        android:layout_width="120dp"
        android:layout_height="120dp"
        android:layout_margin="30dp"
        android:layout_centerHorizontal="true"
        >
    </com.facebook.login.widget.ProfilePictureView>

    <TextView
        android:id="@+id/punUserName"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:textSize="32sp"
        android:textColor="@color/colorAccent"
        android:layout_below="@+id/ppvProfile"
        android:layout_centerHorizontal="true"
        />

    <Button
        android:id="@+id/btnLogOut"
        android:background="@drawable/button_primary"
        android:layout_width="match_parent"
        android:text="@string/logOut"
        android:textColor="@android:color/white"
        android:textSize="16sp"
        android:layout_height="wrap_content"
        android:layout_margin="@dimen/marginUIComponent"
        android:layout_alignParentBottom="true"
        />

    <android.support.v7.widget.RecyclerView
        android:id="@+id/rvSocialContact"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_below="@id/punUserName"
        android:layout_above="@id/btnLogOut"
        >
    </android.support.v7.widget.RecyclerView>


</RelativeLayout>
